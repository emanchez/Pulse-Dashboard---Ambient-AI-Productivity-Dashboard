/** @type {import('next').NextConfig} */
const nextConfig = {
  // No PWA / service-worker plugin is used in this project.
  // If next-pwa or workbox is ever added, configure it here explicitly.
  reactStrictMode: true,
}

module.exports = nextConfig
