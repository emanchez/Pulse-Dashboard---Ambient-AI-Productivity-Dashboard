# PersonalDash Frontend (scaffold)

Quick notes:

- Run `npm install` in `code/frontend`.
- Generate the TypeScript client after the backend is running:

```bash
cd code/frontend
npm run generate:api
```

- Start dev server:

```bash
npm run dev
```
